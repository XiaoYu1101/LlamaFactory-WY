# 通用 JSON 与表格样例改造测试报告

日期：2026-09-10。

## 版本与范围

按要求先将修改前的 JSON 场景版本提交并推送至远端 dev：`07d6444`（feat: generate and review full JSON instruction datasets），然后实施本次通用字段改造。以下改造保留在当前本地工作区。

以样例的全部字段名、值类型及嵌套结构为生成契约，支持自定义字段、system、messages、conversations、数字、布尔、null、对象和数组。生成、人工编辑、数据库持久化、导出均使用完整记录；不再只保存固定三个字段。常见问答与对话自动映射到原生 LlamaFactory，自定义文本字段可手动指定训练角色。

新增可选“表格填写”：常用模板、自定义字段、多个参考样例、对话逐项填写；保留 JSON 粘贴与文件导入，并支持两种模式互相切换。原始外部文件不需要嵌入源码。

## 自动测试

命令：`python -m pytest --confcutdir=tests/intent tests/intent -q`。

结果：**101 passed，7 条依赖弃用提示，无失败**。

新增通用格式覆盖包括：

- 七类记录：四字段问答、自定义问答加嵌套元数据、prompt/completion 加用户 id、顶层 system 加 messages、messages 内系统消息、conversations、自定义字段训练映射。
- 缺字段、多字段、值类型改变、重复 JSON 键、非有限数值、不一致的参考结构均被拒绝。
- 系统与任务上下文配对保护；自定义映射为系统提示的字段也得到保护。
- 保存、生成、人工修改、重置审核状态、保留原始完整记录、导出、重新打开数据库全程字段保真。
- 不支持直接训练的任意 JSON 仍可下载完整记录；自定义映射错误被拒绝。
- 混合格式分场景注册训练数据集，训练参数使用完整数据集列表。
- 模拟分批生成、审核、导出 **1000 条**含 system 的完整记录，数量一致。
- Windows 下导出内容实际字节与文件摘要一致。

原有工作流、接口、挂载及训练桥接检查全部通过。Ruff 检查、TypeScript 检查和 Vite 生产构建通过；构建有单个 JS 包体积提示。

## 真实生成接口

使用本机已配置的真实生成接口，分别提交四字段问答、自定义问答加嵌套元数据、带系统消息的 messages 三类参考样例，每类生成 2 条。

**三次调用均成功，共 6 条有效记录，0 条格式无效记录**。检查全部字段、类型和系统上下文保留。检查文件位于本机 `workspace/generic-live-smoke.json`，密钥和运行数据不提交仓库。

这验证真实接口能按不同结构返回记录，不代表已用新版真实生成并审核 1000 条，也不能证明全部业务答案正确。

## 网页端流程

通过无头 Microsoft Edge 操作实际前端，后端使用临时数据库和明确标识为 browser-qa-mock 的模拟生成器。

JSON 入口测试三种格式，每种均完成场景配置、生成 3 条、编辑、批量审核、冻结版本、浏览器实际下载 JSON：

- system、instruction/input/output，加嵌套 metadata、布尔和 null；编辑后下载值与类型正确。
- messages 系统对话；系统消息保留，人工修改用户消息成功。
- 自定义 sys_context/request_text/result_text，加 flags 数组；通过下拉框配置训练字段，下载原字段不改名。

表格入口测试：

- 选择常用模板，填写系统提示、任务指令、输入与答案；新增数字、布尔、文本数组及自定义文本字段，再删除一个字段。
- 新增并填写第二条参考样例，切换 JSON 再返回表格，内容与类型一致。
- 不编写 JSON，逐项填写两轮 messages 对话并添加自定义 context 字段。
- 两类表格样例各生成 3 条，审核、冻结、实际下载后检查完整字段及类型。
- 输入错误 JSON 后尝试切换表格，提示错误并保留原文本，禁止保存。

所有上述网页检查通过，**没有 JavaScript 页面错误**。截图和下载文件位于本机 `workspace/generic-qa-artifacts/`、`workspace/table-qa-artifacts/`。

## 数据兼容与运行状态

正式工作台仍运行于 http://127.0.0.1:7861/intent/ 。升级前备份为 `workspace/intent-before-generic-json.sqlite3`。逐条比对升级前后已有 **1000 条**样本的内容、原始内容、审核状态和修订号，完全一致；完整 JSON 字段回填也与旧内容一致。API 密钥配置已生效，不显示或提交密钥。

原 LlamaFactory 训练算法和评测逻辑未修改。本次未运行 GPU 微调或真实模型评测。非标准工具调用、多模态、偏好训练格式尚未自动映射为可直接训练的数据集；可以按参考结构生成和导出，再单独适配训练。JSONL 输入统一导出为 JSON 数组，保证记录结构与值类型，不保留原文件的排版方式。


## 后续：env 配置与服务模型发现

生成服务新增 WY_INTENT_* 环境变量覆盖，支持系统环境变量 > .env > JSON 文件；模型名 auto 或空值时发现单个服务模型，多个模型提示手动指定完整 id。私有 IP / localhost 允许无密钥请求；更换地址不会自动复用旧服务密钥。默认禁用新地址上的非通用扩展参数，可显式配置。

新增配置优先级、无鉴权请求、准确模型名传递、多个/空模型列表、手动模型名跳过发现、列表接口失败与重定向、环境变量空密钥优先及公开设置不泄露密钥等检查。完整测试：**115 passed，7 条依赖弃用提示**；前端生产构建通过。

浏览器实测“生成服务配置 → 检测服务模型”，从本机模拟兼容服务返回准确 id，正确显示无需密钥状态，无页面错误。截图 `workspace/model-settings-qa.png`。尚未连接用户另一台机器上的真实 Qwen 服务；实际地址、端口和鉴权需按部署填写。


## 后续：三条参考、跨页审核、原生训练评测入口

当前场景保存与生成要求 4–100 条不同的参考样例。默认每批从用户参考池选 3 条，生成最多 10 条；优先选择历史使用次数少的参考，同等条件随机并尽量避开上一批。每批 seed_ids 入库，续跑继续使用历史统计；模型生成的记录不会加入参考池。

数据审核新增“一键通过全部待审核”和“一键审核并导出 JSON”，覆盖当前版本全部分页，保留驳回与删除状态，逐条记录审核日志。审核并导出使用同一数据库事务，导出验证失败时回滚本次批量审核。

侧栏新增模型训练、模型评测页面；统一启动脚本提供 --full，完整模式保留原生 Train、Evaluate & Predict、Chat、意图分类和 Export。嵌入工作台通过同源消息切换到真实 Gradio 标签。数据版本可填入原生训练控件并切换 Train，准备动作不启动训练。

验证：完整回归 **125 passed**；随后新增原生数据填入事件测试，训练桥接专项 **8 passed**（包含这项新增测试）。共 126 项测试获得通过结果。前端 TypeScript 与生产构建通过。

浏览器验证：三条参考无法保存、四条可保存；生成 40 条后从第二页一键通过所有待审核，再一键下载全部 40 条 JSON，完整字段保持一致；轻量模式训练/评测菜单可见。真实 Gradio 标签容器使用项目导航代码，工作台按钮成功切换 Train 和 Evaluate & Predict；原生训练、评测参数及监控代码通过既有桥接检查。截图和下载文件在 workspace/three-seed-qa/。

现有 1000 条用户数据已备份并保留。当前机器没有 PyTorch 和 transformers，本次未加载真实模型、未进行 GPU 训练和实际模型评测；--full 会给出缺少训练依赖的明确提示。生成 API 不代替本机训练环境。


## 后续：大模型标签推断与持久任务

标签由配置的模型根据完整参考样例和生成要求推断，解析 JSON 不再自动填入启发式标签。任务及开始时的项目、场景草稿入库，页面关闭后可从左侧任务入口恢复。运行中锁定场景表单，完成后允许人工添加、删除标签；修改推断输入后旧结果失效。失败可手动重试，服务重启将未完成任务标记为中断；旧版本草稿保存时拒绝覆盖新的项目版本。

后端完整回归：139 passed，8 条依赖弃用提示。使用模拟模型验证后台执行、客户端关闭、持久化恢复、失败重试、输入失效、结果格式校验、标签顺序与原始样例保持、旧草稿保护。未调用真实模型，未验证实际标签推断质量。本次不启动或重启用户服务；升级需用户自行重启。

离线浏览器回归通过：推断前及运行中禁用控件、关闭页面后恢复运行中任务、恢复完成结果、手动新增标签在刷新及保存后重新打开时保留、修改输入后结果失效。使用请求拦截提供模拟接口，没有启动或连接用户服务，没有页面 JavaScript 错误。截图位于 workspace/label-task-qa/。前端 TypeScript 与生产构建通过，Ruff 与 git diff --check 通过；构建仍提示前端主包偏大。


## 后续：LoRA 参数与训练迁移包

新增模型训练配置表单，提供模型路径、模板及常用 LoRA/训练参数；参数校验、保存与恢复通过独立 API 和工作区持久存储实现。支持下载包含固定训练数据、字段映射、train_config.json、启动脚本的迁移包，使用原生 LlamaFactory CLI 训练。完整模式可将保存的参数填入原生 Train 控件，不自动启动训练。

完整回归：154 passed，9 条依赖弃用提示。新增检查覆盖参数边界、模板注册表、数据校验、保存恢复、system 字段保留、迁移后相对路径解析、dry-run、自动精度、防止覆盖既有输出，以及真实 Runner 参数解析器对自定义 LoRA 参数的接收。硬件和训练进程调用均为模拟，不进行实际 GPU 训练。

离线浏览器验证：填写自定义参数、保存和下载、刷新页面恢复配置、修改后禁止下载旧配置，全部通过，无页面 JavaScript 错误。截图 workspace/training-config-qa/form.png。前端 TypeScript/生产构建、Ruff 和 git diff --check 通过；仍有前端主包体积提示。本次没有启动或重启用户程序。


## 上传前最终接口复查

完整隔离回归：158 passed，10 条依赖弃用提示。新增原生评测数据一键填入，验证数据目录、登记名称、样本数和唯一输出目录正确传递，并保留用户选择的模型和 LoRA 适配器。训练与评测数据填入在已有任务运行时均拒绝替换。原生数据登记器显式读取 UTF-8，避免 Windows 中文字段编码问题；ShareGPT 支持根 system_prompt，并在根系统字段与首条系统消息冲突时拒绝自动映射，防止静默丢失上下文。

最终生产构建、Ruff、补丁格式检查和训练表单/标签任务离线浏览器回归通过。未运行真实模型训练与评测，未启动用户服务；真实硬件、模型加载及指标质量需要目标机器验证。提交不包含 .env、用户样例和 workspace 数据。
